spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3308/INZYNIERKA_TEST?serverTimezone=Europe/Amsterdam&createDatabaseIfNotExist=true}
spring.jpa.hibernate.ddl-auto= create-drop
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:root}
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect
spring.jpa.show-sql=true

spring.flyway.url=${FLYWAY_DB_URL:jdbc:mysql://localhost:3308}
spring.flyway.schemas=${FLYWAY_BD_SCHEMA:INZYNIERKA_TEST}
spring.flyway.user=${DB_USERNAME:root}
spring.flyway.password=${DB_PASSWORD:root}

jwt.secret=${JWT_SECRET:test123}
jwt.validity=${JWT_VALIDITY:100000}

spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.username=${MAIL_USER:inzmichu2@gmail.com}
spring.mail.password=${MAIL_PASS:inzhaslo}
spring.mail.port=${MAIL_PORT:587}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
